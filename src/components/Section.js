import React from 'react';
import Contacts from './materialui/Contact';

const Section = () => {
    return (
        <div className="section">
            <Contacts/>
        </div>
    );
};

export default Section;